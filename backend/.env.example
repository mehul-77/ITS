# Backend environment variables (for Render/Railway/Heroku deployment)

# Port (usually set by hosting provider)
PORT=8000

# CORS settings
ALLOWED_ORIGINS=*

# Optional: Database or cache settings (for future expansion)
# DATABASE_URL=
# REDIS_URL=
